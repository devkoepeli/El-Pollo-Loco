class Counter {
    counter = 0;
    x;
    y;
 
    /**
     * set x/y coordinates for placement in canvas
     * @param {number} x - stands for the x-coordinate
     * @param {number} y - stands for the y-coordinate
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}